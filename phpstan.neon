includes:
    - vendor/larastan/larastan/extension.neon
    - vendor/nesbot/carbon/extension.neon

parameters:

    paths:
        - app/

    level: 5

    errorFormat: github

    ignoreErrors:
        -
            message: '#PHPDoc tag @mixin contains unknown class App\\Models\\[a-zA-Z0-9\\_]+.#'
            path: app